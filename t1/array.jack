class array {
function array new(in int size)
{
	var array a;
	a := std.alloc(size);
	a[-1] := size;
	return a;
}

method void dispose()
{
	std.free(this);
	return;
}

method int get_size()
{
	var array a;
	a := this;
	return a[-1];
}

} // class

