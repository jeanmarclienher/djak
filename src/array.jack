class array {
constructor array new(in int size)
{
	var array a;
	var array t;
	t := this;
	a := std.alloc(size);
	a[-1] := t[-1]; // class ID
	std.free(t);
	return a;
}

virtual void dispose()
{
	std.free(this);
	return;
}
} // class

